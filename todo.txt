(C) 2026-09-17 vitest を 5.0 系へ上げる。vitest / @vitest/coverage-v8 / @vitest/browser-playwright は同時更新が必須(対象: package.json, pnpm-lock.yaml, vitest.config.ts。@storybook/addon-vitest の peer 整合も要確認。検証: mise run ci が通り node/dom/storybook の 3 project すべてが緑になること) +deps
(C) 2026-09-17 GitHub Actions の major 更新を取り込む: actions/checkout v4->v7, actions/setup-node v4->v7, actions/cache v4->v6, pnpm/action-setup v4->v6。pnpm update は major を跨がないため個別対応が要る(対象: .github/workflows/ci.yml, .github/workflows/pr-insights-labeler.yml, .github/actions/setup-node-env/action.yml, .github/actions/setup-pnpm-cache/action.yml。検証: 各 action の release note で breaking change を確認し、PR で CI の全 job が通ること) +ci
(D) 2026-09-17 pnpm を 12 系へ上げる。packageManager の変更は CI と全開発環境へ波及するため単独で扱う(対象: package.json の packageManager, pnpm-lock.yaml, .github/actions/setup-pnpm-cache/action.yml。検証: pnpm install --frozen-lockfile と mise run ci がローカルと GitHub Actions の両方で通ること) +deps
(B) 2026-09-17 拡張 popup のブラウザズーム時の切れ落ちを直す。Chrome は popup を最大 800x600 の画面ピクセルに収めるため、ズーム時の CSS ビューポートはそれより小さくなり、body に固定 height/width を置くと右端と下端が overflow: hidden で切り落ちて下部コントロール(モデル選択など)が可視領域外に出る。body 自身へ vh/vw を入れる素朴な対処は循環参照になり popup が最小サイズへ潰れるので不可(2026-09-17 に実際に発生、Arc と Dia の両方で再現)。body の height は固定値のまま、vh 制約は内側のスクロールコンテナ側へ移す方針(対象: src/styles/base.css, src/styles/layout.css, src/styles/tokens/semantic.css。起点となる差分は tmp/parallel-session/base-css-viewport-fix.patch に保全済み。browser-toolkit-47 セッションから引き継いだタスク。検証: 拡張をリロードせず popup を開き、ブラウザズーム 80%/100%/125% で下部コントロールが切れず、かつ popup が潰れないこと) +ui
(D) 2026-09-17 検索結果ブロックリストで「読めないルールを一括削除する」操作を足す。id を取り出せないほど形状が壊れた storage エントリは skippedCount に数えられるが popup の一覧に出ず、個別削除もできない。mutation はブロックしないので行き止まりではないが、storage に不可視のゴミが残り corrupted バナーが出続ける(対象: src/popup/panes/SearchBlocklistPane.tsx, src/popup/panes/search-blocklist/useSearchBlocklistRules.ts, src/background/runtime_handlers.ts, src/search-blocklist/rules.ts, src/i18n/resources.ts。判断の経緯は plans/search-blocklist/design.md の「partition 実装後の既知の限界と判断」。検証: 形状不正エントリのみを storage に置いた状態から一括削除でバナーが消えること) +ui
(A) 2026-09-17 設定 pane のモデル選択が Arc/Dia でクリックしても候補を出さない。browser-toolkit-47 から引き継いだ。同じ dist で Chromium は正常に開くことが Playwright + 実拡張で自動観測済み(click 後 aria-expanded=true、.mbu-select-positioner が rect y=474-556/800x600 で存在、.mbu-select-item 2 件、console/pageerror 0 件)。trigger を可視下端付近に置いた場合やビューポート 695x300 まで縮めた場合も正常なので body の overflow: hidden による clip 説は否定済み。拡張リロード後も Arc で再現するため古い dist 説も否定済み。残る仮説は (1) Arc/Dia 固有の popup 描画で Portal がパネル外にクリップされる (2) Arc がパネルを再レイアウトする際に Base UI の Positioner が outside-press 判定で閉じる (3) Arc 固有の React クラッシュ(対象: src/components/shared/Select.tsx, src/popup/panes/settings/SettingsModelSection.tsx。@base-ui/react 1.8.0 の Select は Portal + Positioner + alignItemWithTrigger 既定 true。分岐: aria-expanded が false なら open state へ入っていない、true なのに positioner が無い/寸法 0 なら描画側。検証: Arc の popup で DevTools を開き click 直後の console / .mbu-select-positioner の rect / aria-expanded を取る。回帰防止として Storybook play で select を開く story も追加する) +ui +bug
