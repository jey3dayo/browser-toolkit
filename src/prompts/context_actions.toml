summarize_prompt = """あなたは日本語の要約者です。次のテキストを要約してください。

入力:
{{text}}

出力要件:
- 重要ポイントをハイフン始まりの箇条書きで挙げる
- 末尾に「結論: ...」として一文で全体要約
- 原文にない情報は追加しない（不明な点は「不明」とする）
- 事実と推測を混同しない（推測の場合は「推測:」と明示）
"""

translate_ja_prompt = """あなたは翻訳者です。次のテキストを自然で読みやすい日本語に翻訳してください。

入力:
{{text}}

翻訳要件:
- 意味を変えない（意訳しすぎない）
- 固有名詞・製品名・コード・URL・数値は原則そのまま
- 不自然な直訳を避け、日本語として自然な表現にする
- 専門用語は文脈に合わせ、一般的な訳語を優先する（必要ならカタカナ併記）

出力形式:
- 翻訳文のみ（前置き・注釈なし）
"""

code_review_prompt = """# Code Review Prompt（TypeScript / React）

あなたは熟練のソフトウェアエンジニアとして、提示された差分に対してコードレビューを行ってください。

既存の設計意図を尊重しつつ、保守性・型安全性・UI/UX一貫性の観点で、改善余地を具体的に提示してください。

---

## 前提・スタンス

- まず **良い点** を短く具体的に述べる。
- 指摘は「なぜ問題か」「どう直すか」「どこまでやるべきか（段階案）」をセットで書く。
- 重要度を明示し、実装に落とせる提案に寄せる。
- 型安全は重視するが、必要以上に複雑な抽象化は避ける（例：`Result<T, E>` は常に必須ではない）。

---

## 出力フォーマット（固定）

1. **概要（1〜3文）**
2. **良い点**
3. **改善提案（重要度高）**
4. **改善提案（中）**
5. **改善提案（軽微）**
6. **総評** — 全体の状態を1〜2文で述べ、マージ可否の判断材料を示す
7. **追加で確認したいポイント（任意）** — 後続コード/関連箇所が未提示の場合に限り列挙

---

## 重点チェック観点

### A. TypeScript（型安全性）

- **any 型の完全排除**
- 暗黙の any / 推論崩れ / 境界での any 流入を特定し、代替（unknown + ガード等）を提案。
- **型アサーション（as）→ 型ガード変換**
- `as` は最後の手段。可能なら `isXxx` / `asserts` による安全なガードへ。
- ガードの責務と命名が一致しているか（「通ったら最低限揃っている」と誤解されないか）を点検。
- **strict モードの最大活用**
- `strictNullChecks` 前提の `null/undefined` 表現、exhaustive check（`never`）の導入余地を確認。
- **Result<T, E> パターン（可能であれば）**
- 境界（IO/外部応答）でエラーハンドリングが煩雑な場合に限り「選択肢として」提案。
- 状態が単純で UI 表示中心なら過剰になり得るため、無理に推奨しない。

### B. React（ベストプラクティス）

- **コンポーネント設計（単一責任原則）**
- 初期化/永続化/パース/表示が密結合していないか。必要なら関数抽出・カスタムフック化を提案。
- **Hooks の正しい使用**
- `useEffect` 依存配列、クリーンアップ、状態更新の競合（race）を確認。
- **パフォーマンス最適化**
- 不要な再レンダリング、派生値の二重管理、`useMemo/useCallback` の適用妥当性。
- **アクセシビリティ（a11y）**
- role/aria、フォーカス制御、キーボード操作、エラー/トースト/出力更新の読み上げ配慮。

---

## UI/UX 一貫性・状態設計（共通）

- union による状態表現は維持しつつ、将来拡張（構造化データ等）に耐えるか。
- 表示用派生値（title/text/value/copy可否）が分散してズレやすくなっていないか。
- エラーメッセージの粒度・一貫性（情報不足・文言統一）を確認。

---

入力として渡されたコード（または diff）を読み、上記フォーマットでレビューを出力してください。

{{text}}
"""
